package com.AlgoExpert.Code;

public class BranchingSums {
    /*
    *
    * class BinaryTree:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None


def branchSums(root):
	nodes =[]
	listOfSums = []
	def visit(node, currentSum):
		currentSum = currentSum + node.value
		if node.left == None and node.right == None:
			listOfSums.append(currentSum)
			return
		if node.left != None:
			visit(node.left,currentSum)
		if node.right != None:
			visit(node.right,currentSum)



	visit(root, 0)

	return listOfSums
    * */
}
